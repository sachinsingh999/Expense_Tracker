{
  "buildCommand": "if [ -d \"client\" ]; then cd client && npm install && npm run build; else npm run build; fi",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
